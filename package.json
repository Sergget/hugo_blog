{
  "devDependencies": {
    "fs-extra": "^11.4.0",
    "gray-matter": "^4.0.3",
    "honkit": "^6"
  },
    "dependencies": {
    "honkit-plugin-mermaid-hybrid": "^1.0.2"
  },
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:fast": "node scripts/dev.js --skip-books",
    "build:books": "node scripts/build-books.js",
    "build:site": "hugo",
    "build": "npm run build:books && npm run build:site",
    "diary": "node scripts/diary.js",
    "sync": "node scripts/sync.js ../obsidian_vault ./content/posts"
  }
}
